version: "3.8"

services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - type: bind
        source: ./instance
        target: /app/instance
    env_file:
      - .env
    environment:
      - SQLALCHEMY_DATABASE_URI=${DB_URI}
      - FLASK_SECRET_KEY=${FLASK_SECRET_KEY}
    restart: always
